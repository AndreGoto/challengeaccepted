<%#= simple_form_for(@challenge_message,url: challenge_challenge_messages_path(@challenge.id), remote: false) do |f| %>
  <%#= f.input :message, label: "Trash Talking", input_html: {id: "chat_speaker_input"} %>
  <%#= f.button :submit, value: "Send", class: "btn btn-primary", 'data-behavior': "chat_speaker" %>
<%# end %>
<form>
  <div class="form-group">
    <textarea  id="chat_speaker_member_txt" data-behavior="chat_speaker" cols="10" rows="5" class="form-control" placeholder="Write something here :)"></textarea>
  </div>
  <div class="form-group">
    <input type="hidden" id="chat_speaker_member_id" value="<%= @current_member.id %>">
    <input type="button" id="chat_speaker_member_button" value="Send" class="btn btn-primary btn-block">
  </div>
</form>
